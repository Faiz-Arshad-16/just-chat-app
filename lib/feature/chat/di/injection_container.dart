import 'package:get_it/get_it.dart';

final GetIt sl = GetIt.instance;

void setup() {
  // Register chat feature dependencies here
  // Example: locator.registerLazySingleton<ChatRepository>(() => ChatRepositoryImpl());
}