import 'dart:ui';

class AppColors {
  /// Primary background color for app screens
  static const Color backgroundColor = Color(0xFF474779);
  /// Primary text color for UI elements
  static const Color textColor = Color(0xFFFFFFFF);
  /// Secondary text color for hints and subtitles
  static const Color secondaryTextColor = Color(0xFF7E82A0);
  /// App bar background color
  static const Color appBarColor = Color(0xFF353567);
  /// Color for sent messages and buttons
  static const Color sendMessageColor = Color(0xFF7C7CDF);
  /// Color for received messages
  static const Color receiveMessageColor = Color(0xFF585988);
  /// Color for buttons
  static const Color buttonColor = Color(0xFF7C7CDF);
}